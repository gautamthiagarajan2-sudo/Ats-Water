export interface SystemItem {
  id: string;
  title: string;
  description: string;
  image: string;
}

export interface ProductItem {
  name: string;
  category: string;
  desc?: string;
}

export interface IndustryItem {
  name: string;
  image: string;
}
